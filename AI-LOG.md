| Где использовал | Промпт (кратко) | Что переписал руками и почему |
|---|---|---|
| Планирование декомпозиции и логики загрузки | «Как лучше разделить приложение на компоненты? Стоит ли вынести логику в composable или создать новый?» | Использовал предложенное разделение ответственности. Вручную написал composable `useUsers` для управления загрузкой пользователей и функцию фильтрации списка в `App.vue`; добавил компоненты состояний `Loader` и `ErrorState`. |
| Перенос существующего интерфейса в компоненты | «Перенеси существующую разметку в новые компоненты, реализуй props и emits и подключи компоненты в `App.vue`.» | Вручную убрал лишние стили после переноса разметки, чтобы стили не дублировались. |
| Создание компонентов состояний | «Добавь компоненты `ErrorState` и `Loader`.» | Вручную поправил props компонентов и их передачу из `App.vue`, чтобы интерфейсы компонентов соответствовали используемым данным. |
